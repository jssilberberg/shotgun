{
  "name": "shotgun",
  "private": true,
  "type": "module",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "test": "node --test server/test/gameEngine.test.ts server/test/llmHostGrader.test.ts server/test/questionProvider.test.ts server/test/configEnv.test.ts",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w client",
    "lint": "npm run lint -w shared && npm run lint -w server && npm run lint -w client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=24"
  }
}
